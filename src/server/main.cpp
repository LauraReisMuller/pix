// Inicia os threads do servidor (Descoberta, Processamento, Interface)
#include <stdio.h>

int main() {
    printf("Servidor iniciado.\n");
    // Inicialização dos threads de Descoberta, Processamento e Interface
    return 0;
}
